const Universal = (name) => {
    // console.log(name)
  return (
    <div className="button_div">
    <button>{name.Join}</button>
    <button>{name.Sett}</button>
    <button>{name.Login}</button>
    <button>{name.Cont}</button>
    <button>{name.Sear}</button>
    <button>{name.Hel}</button>
    <button>{name.Hom}</button>
    <button>{name.Down}</button>
    </div>
  );
};

export default Universal;
